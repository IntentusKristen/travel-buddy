/**
 * Events that can be triggered by a `Button` instance.
 */
export declare const enum ButtonEvent {
    /** Fired when the button is selected. */
    SELECT = "select",
    /** Fired when the button is deselected. */
    DESELECT = "deselect"
}
