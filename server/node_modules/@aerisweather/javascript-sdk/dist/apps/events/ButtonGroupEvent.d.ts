/**
 * Events that can be triggered by a `ButtonGroup` instance.
 */
export declare const enum ButtonGroupEvent {
    /** Fired when the button group is expanded. */
    EXPAND = "expand",
    /** Fired when the button group is collapsed. */
    COLLAPSE = "collapse"
}
