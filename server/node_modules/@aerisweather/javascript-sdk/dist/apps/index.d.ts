import Account from '../account/Account';
import ButtonPanel from './views/ButtonPanel';
import InfoPanel from './views/InfoPanel';
import TimelinePanel from './views/TimelinePanel';
import LegendPanel from './views/LegendPanel';
import PlaceSearchPanel from './views/PlaceSearchPanel';
import Animation from '../maps/anim/Animation';
import { ButtonPanelViewOptions, InfoPanelOptions, LegendPanelOptions, PlaceSearchPanelOptions, TimelinePanelOptions, TooltipOptions } from './types';
import { InteractiveMapAppOptions } from './types/mapapp.interface';
import { Servers } from '../types/request.interface';
import Tooltip from './ui/Tooltip';
/**
 * The `Apps` module provides the public interface for all application components that are part
 * of the SDK.
 */
export declare module Apps {
    /**
     * Sets the Aeris account instance to use with the `Views` module.
     *
     * @param {Account} account
     */
    const setAccount: (account: Account) => void;
    /**
     * Sets the base paths for the API servers to use with data and image requests.
     *
     * @param {Servers} servers
     */
    const setServers: (servers: Servers) => void;
    /**
     * Initializes and returns a new `InteractiveMapApp` instance.
     *
     * @param {*} target
     * @param {InteractiveMapAppOptions} [opts]
     * @param {(map: Map) => void} [onInit]
     * @returns {InteractiveMap}
     */
    const InteractiveMapApp: (target: any, opts?: InteractiveMapAppOptions) => InteractiveMapApp;
    const panels: {
        LayersPanel: (opts?: ButtonPanelViewOptions) => ButtonPanel;
        InfoPanel: (opts?: InfoPanelOptions) => InfoPanel;
        TimelinePanel: (timeline: Animation, opts?: TimelinePanelOptions) => TimelinePanel;
        LegendPanel: (opts?: LegendPanelOptions) => LegendPanel;
        PlaceSearchPanel: (opts?: PlaceSearchPanelOptions) => PlaceSearchPanel;
    };
    const ui: {
        Tooltip: (content: string, opts?: TooltipOptions) => Tooltip;
    };
}
