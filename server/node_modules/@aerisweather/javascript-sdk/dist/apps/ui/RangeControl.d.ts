import View from './View';
import { NumericRange, RangeControlOptions, RangeItem } from '../types';
declare class RangeControl extends View<RangeControlOptions> {
    private _range;
    private _intervals;
    get intervals(): RangeItem[];
    constructor(opts?: RangeControlOptions);
    setIntervals(intervals: RangeItem[] | number[]): void;
    setRange(min: number, max: number, interval: number): void;
    value(range?: NumericRange): any;
    protected _render(): string;
    protected _setupEvents(): void;
    private _handleClick;
    private _select;
}
export default RangeControl;
