import Component from './Component';
import { ComponentOptions, ComponentState } from '../types';
/**
 * A data type that represents the configuration options for a slider handle.
 */
interface HandleOptions extends ComponentOptions {
    values: {
        min: number;
        max: number;
        current: number;
    };
    disabled?: boolean;
    tabindex?: number;
}
interface HandleState extends ComponentState {
    clickFocused: boolean;
}
declare class SliderHandle extends Component<HandleOptions, HandleState> {
    constructor(opts: HandleOptions);
    initialize(data?: any): void;
    setClickFocus(focused: boolean): void;
    clickFocus(): void;
    focus(): void;
    blur(): void;
    updatePosition(offset: number, opts?: HandleOptions): void;
    protected _render(data: any): string;
    protected _afterRender(): void;
    private _handleMouseDown;
    private _handleMouseUp;
    private _handleBlur;
}
export default SliderHandle;
