import Button from './buttons/Button';
import { ComponentState, SwitchOptions } from '../types';
export interface SwitchState extends ComponentState {
    selected: boolean;
}
declare class Switch extends Button<SwitchOptions, SwitchState> {
    /**
     * Current value based on the switch selected state.
     *
     * @readonly
     * @type {*}
     * @memberof Switch
     */
    get value(): any;
    /**
     * Initializes and returns a switch control instance with the specified configuration options.
     *
     * @param {*} [opts]
     * @memberof Switch
     */
    constructor(opts?: any);
    protected _render(data?: any): string;
}
export default Switch;
