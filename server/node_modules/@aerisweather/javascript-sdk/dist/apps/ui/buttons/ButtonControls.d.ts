import View from '../View';
import { ViewOptions } from '../../types';
interface ButtonControlsOptions extends ViewOptions {
    controls: {
        [key: string]: any;
    };
    onChange?: (value: any) => void;
}
declare class ButtonControls extends View<ButtonControlsOptions> {
    private _indicator;
    private _popover;
    private _controls;
    private _onChange;
    constructor(opts?: ButtonControlsOptions);
    setLoading(loading: boolean): void;
    addToButton(button: any): void;
    protected _render(): string;
    protected _setupEvents(): void;
    private _showPopover;
    private _hidePopover;
    private _handleWindowClick;
}
export default ButtonControls;
