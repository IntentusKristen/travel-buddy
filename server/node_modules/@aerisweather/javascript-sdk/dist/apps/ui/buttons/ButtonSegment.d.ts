import { ButtonSegmentOptions, ButtonState } from '../../types/button.interface';
import Button from './Button';
declare class ButtonSegment extends Button<ButtonSegmentOptions, ButtonState> {
    constructor(opts?: any);
    get key(): string;
    get multiselect(): boolean;
    protected _render(data?: any): string;
}
export default ButtonSegment;
