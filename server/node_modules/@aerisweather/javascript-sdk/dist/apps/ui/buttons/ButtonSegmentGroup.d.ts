import Component from '../Component';
import ButtonSegment from './ButtonSegment';
import { ButtonSegmentGroupOptions } from '../../types/button.interface';
import { ComponentState } from '../../types';
declare class ButtonSegmentGroup extends Component<ButtonSegmentGroupOptions, ComponentState> {
    private storedSegments;
    constructor(opts?: ButtonSegmentGroupOptions);
    get id(): string;
    get parentId(): string;
    get title(): string;
    get value(): string | Array<string>;
    get segments(): ButtonSegment[];
    setSegments(segments: ButtonSegment[]): void;
    selectedSegments(): ButtonSegment[];
    clear(triggerEvents?: boolean): void;
    private _handleSelectSegment;
    private _handleDeselectSegment;
    private _handleValueChange;
    protected _render(data?: any): string;
    protected _onAddedToDOM(): void;
}
export default ButtonSegmentGroup;
