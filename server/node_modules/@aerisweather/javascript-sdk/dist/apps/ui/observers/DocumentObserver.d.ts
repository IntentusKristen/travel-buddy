import EventDispatcher from '../../../event/EventDispatcher';
declare class DocumentObserver extends EventDispatcher {
    private eventHandlers;
    constructor(events: string | string[]);
    start(): void;
    stop(): void;
}
export default DocumentObserver;
