import { ComponentState, ViewOptions } from '../types';
import Component from '../ui/Component';
export interface LayerControlOptions {
    type: 'slider';
    id?: string;
    label: string;
    options?: any;
}
interface LayerControlsOptions extends ViewOptions {
    controls: LayerControlOptions[];
}
declare class LayerControlsView extends Component<LayerControlsOptions, ComponentState> {
    constructor(opts?: LayerControlsOptions);
    protected _render(data?: any): string;
    protected _setupEvents(): void;
}
export default LayerControlsView;
