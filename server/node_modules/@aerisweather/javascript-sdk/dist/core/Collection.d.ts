declare class Collection<T> {
    private store;
    get items(): {
        [key: string]: T;
    };
    get count(): number;
    has(key: string): boolean;
    get(key: string): T;
    add(key: string, item: T): void;
    remove(key: string): void;
    each(fn?: (key: string, item: T) => void): void;
}
export default Collection;
