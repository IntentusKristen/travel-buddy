import Account from '../account/Account';
interface ValidTime {
    time: Date;
    min: Date;
    max: Date;
    run: Date;
}
declare class LayerTimes {
    account: Account;
    constructor(account: Account);
    times(layer: string, limit?: number): Promise<ValidTime[]>;
    timesInRange(layer: string, from: Date, to: Date, limit?: number): Promise<ValidTime[]>;
}
export default LayerTimes;
