import Collection from '../../core/Collection';
declare class DataSourceStore extends Collection<any> {
    private _order;
    private _mapping;
    get mapping(): Map<string, string>;
    get order(): Map<string, number>;
    add(key: string, source: any, order?: number): void;
    get(key: string): any;
}
export default DataSourceStore;
