import { LayerOptions } from '../types';
import { LayerStyle, LayerStyleGroups } from '../types/layer.type';
import { MarkerStyleProvider } from '../types/style.type';
/**
 * Class that manages the configuration and style providers based on layer code.
 *
 * @export
 * @class MapLayerConfig
 */
declare class MapLayerConfig {
    private _configs;
    /**
     * Initializes and returns a map configuration instance with the provided styles.
     *
     * @param {({ [key: string]: LayerStyleGroups | LayerStyle })} [styles]
     * @memberof MapLayerConfig
     */
    constructor(styles?: {
        [key: string]: LayerStyleGroups | LayerStyle;
    });
    /**
     * Returns the layer configuration for the specified layer code.
     *
     * @param {string} layer
     * @returns {LayerOptions}
     * @memberof MapLayerConfig
     */
    getConfig(layer: string): LayerOptions;
    setConfig(layer: string, config: LayerOptions): void;
    /**
     * Sets the style(s) for the specified layer code.
     *
     * @param {string} layer
     * @param {(LayerStyleGroups | LayerStyle)} style
     * @memberof MapLayerConfig
     */
    setStyle(layer: string, style: LayerStyleGroups | LayerStyle): void;
    /**
     * Sets the style(s) for the specified layers.
     *
     * @param {({ [layer: string]: LayerStyleGroups | LayerStyle })} layers
     * @memberof MapLayerConfig
     */
    setStyles(layers: {
        [layer: string]: LayerStyleGroups | LayerStyle;
    }): void;
    /**
     * Returns the style associated with the specified layer code, if any.
     *
     * @param {string} layer
     * @param {(( 'raster' | 'marker' | 'polygon' | 'polyline'))} type
     * @returns {LayerStyle}
     * @memberof MapLayerConfig
     */
    getStyle(layer: string, type: ('raster' | 'marker' | 'polygon' | 'polyline')): LayerStyle;
    /**
     * Sets the text style to use for text annotations.
     *
     * @param {ShapeStyle} style
     * @memberof MapLayerConfig
     */
    setTextStyle(style: MarkerStyleProvider): void;
    /**
     * Returns the text style.
     *
     * @returns {ShapeStyle}
     * @memberof MapLayerConfig
     */
    getTextStyle(): MarkerStyleProvider;
}
export default MapLayerConfig;
