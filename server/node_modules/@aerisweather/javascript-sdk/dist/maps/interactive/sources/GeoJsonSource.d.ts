import VectorSource from './VectorSource';
import { LayerDataType } from '../../types/layer.type';
/**
 * `GeoJsonSource` is an object that is responsible for loading data and managing a series of
 * vector elements on a map view from GeoJSON data.
 */
declare class GeoJsonSource extends VectorSource {
    protected _process(data: any, parentId?: string): LayerDataType;
}
export default GeoJsonSource;
