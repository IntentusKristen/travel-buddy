import ImageAnimation from '../../anim/ImageAnimation';
import TileAnimation, { TileAnimationProvider } from '../../anim/TileAnimation';
import { TileAnimationOptions, TiledLayer } from '../../types';
import TileSource from './TileSource';
declare class TileDataAnimator implements TileAnimationProvider {
    readonly animation: TileAnimation | ImageAnimation;
    readonly source: TileSource;
    constructor(source: TileSource, opts: TileAnimationOptions);
    /** Animation Provider */
    animationLayerForDate(animation: TileAnimation, date: Date, wait?: boolean): Promise<TiledLayer>;
}
export default TileDataAnimator;
