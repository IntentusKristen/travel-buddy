import DataAnimation, { DataAnimationProvider } from '../../anim/DataAnimation';
import { AnimatableSource, AnimationOptions, TimeRepresentable } from '../../types';
import VectorSource from './VectorSource';
declare class VectorDataAnimator implements AnimatableSource<DataAnimation>, DataAnimationProvider {
    readonly animation: DataAnimation;
    readonly source: VectorSource;
    private _animatableData;
    private _nonAnimatableData;
    constructor(source: VectorSource, opts: AnimationOptions);
    /**
     * Animation Provider
     */
    animationData(animation: DataAnimation): Promise<TimeRepresentable[]>;
}
export default VectorDataAnimator;
