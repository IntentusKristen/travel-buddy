import { Coordinate, GeoBounds } from '../../../types';
import { MultiPolygon, Polygon } from '../../geo';
import { MarkerStyle, OverlayFactory } from '../../types';
import { ShapeStyleProvider } from '../../types/style.type';
import { MarkerMapElement, PolygonMapElement, PolylineMapElement } from '../MapElement';
declare type LineSegment = {
    coords: Coordinate[];
    bounds: GeoBounds;
    data?: any;
};
export declare const generateMarker: (key: string, record: any, coord: Coordinate, style: MarkerStyle, factory: OverlayFactory) => MarkerMapElement;
export declare const generatePolyline: (key: string, record: any, points: Coordinate[], style: ShapeStyleProvider, factory: OverlayFactory) => PolylineMapElement;
export declare const generatePolygon: (key: string, record: any, geometry: Polygon | MultiPolygon, style: ShapeStyleProvider, factory: OverlayFactory) => PolygonMapElement;
export declare const generateLineSegments: (points: any, groupKeyPath: string, coordFn: (point: any) => any) => LineSegment[];
export {};
