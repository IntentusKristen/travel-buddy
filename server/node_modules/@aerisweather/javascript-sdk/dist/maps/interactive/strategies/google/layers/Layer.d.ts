/// <reference types="googlemaps" />
import MapLayer, { MapLayerOptions } from '../../../layers/MapLayer';
export interface GoogleLayerOptions extends MapLayerOptions {
    zIndex?: number;
}
/**
 * `Layer` is a container class that is responsible for creating and managing a Google Maps
 * source and associated layer representation on a Google Maps map.
 */
declare class GoogleLayer<T extends GoogleLayerOptions, S> extends MapLayer<google.maps.Map, T, S> {
    invalidate(): void;
    protected createRenderable(): S;
    protected updateVisibility(visible: boolean): void;
    protected addToMap(map: google.maps.Map): void;
    protected removeFromMap(map: google.maps.Map): void;
}
export default GoogleLayer;
