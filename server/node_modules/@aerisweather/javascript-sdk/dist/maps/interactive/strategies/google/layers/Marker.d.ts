/// <reference types="googlemaps" />
import { MarkerOptions, MarkerStyle } from '../../../../types';
import MapMarker, { MapMarkerOptions } from '../../../layers/MapMarker';
export interface GoogleMarkerOptions extends MapMarkerOptions {
}
declare class GoogleMarker extends MapMarker<google.maps.Map, GoogleMarkerOptions, google.maps.Marker> {
    update(opts: MarkerOptions): void;
    setStyle(style: MarkerStyle): void;
    protected createRenderable(): google.maps.Marker;
    protected updateVisibility(visible: boolean): void;
    protected addToMap(map: google.maps.Map, index?: number): void;
    protected removeFromMap(map: google.maps.Map): void;
}
export default GoogleMarker;
