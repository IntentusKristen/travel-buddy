/// <reference types="googlemaps" />
import { PolygonDef } from '../../../../types/layer.type';
import GoogleLayer from './Layer';
export interface GooglePolygonLayerOptions extends google.maps.PolygonOptions {
}
declare class GooglePolygonLayer extends GoogleLayer<GooglePolygonLayerOptions, google.maps.Polygon[]> {
    private _polygons;
    constructor(layer: string, polygons: PolygonDef[], opts?: GooglePolygonLayerOptions, id?: string);
    setOpacity(value: number): void;
    protected createRenderable(): google.maps.Polygon[];
    protected addToMap(map: google.maps.Map): void;
    protected removeFromMap(map: google.maps.Map): void;
}
export default GooglePolygonLayer;
