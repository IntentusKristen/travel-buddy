/// <reference types="googlemaps" />
import GoogleLayer, { GoogleLayerOptions } from './Layer';
interface GoogleTileLayerOptions extends GoogleLayerOptions {
    url?: string;
}
declare class GoogleTileLayer extends GoogleLayer<GoogleTileLayerOptions, google.maps.ImageMapType> {
    setOpacity(value: number): void;
    setZIndex(index: number): void;
    protected updateVisibility(visible: boolean): void;
    setUrl(url: string): void;
    invalidate(): void;
    protected createRenderable(): google.maps.ImageMapType;
    protected addToMap(map: google.maps.Map): void;
    protected removeFromMap(map: google.maps.Map): void;
}
export default GoogleTileLayer;
