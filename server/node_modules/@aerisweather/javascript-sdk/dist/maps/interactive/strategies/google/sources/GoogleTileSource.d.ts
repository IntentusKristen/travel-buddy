/// <reference types="googlemaps" />
import TileSource from '../../../sources/TileSource';
import TileAnimation from '../../../../anim/TileAnimation';
import MapRequest from '../../../../../network/maps/MapRequest';
import GoogleTileLayer from '../layers/TileLayer';
import { TileSourceOptions, TiledLayer } from '../../../../types';
/**
 * `GoogleTileSource` is a subclass of `TileSource` that creates and manages a single tile layer to
 * be used with an `GoogleMapStrategy`.
 *
 * @export
 * @class LeafletTileSource
 * @extends {TileSource}
 */
declare class GoogleTileSource extends TileSource {
    panes: google.maps.MapPanes;
    node: Element;
    /** Keeps track of the currently loading tile source for animation so it can be removed on reset */
    private _loadingSource;
    private _layerRemovedWhileLoading;
    constructor(key: string, service: MapRequest, opts?: TileSourceOptions);
    get overlay(): GoogleTileLayer;
    set timestamp(value: Date);
    /**
     * Shows the layer associated with the source.
     *
     * @memberof GoogleTileSource
     */
    show(): void;
    /**
     * Hides the layer associated with the source.
     *
     * @memberof GoogleTileSource
     */
    hide(): void;
    /**
     * Flags the source for removal from the managing map strategy.
     *
     * @memberof GoogleTileSource
     */
    remove(): void;
    /**
     * Changes the opacity of source's layer.
     *
     * @param {number} value
     * @memberof GoogleTileSource
     */
    setOpacity(value: number, update?: boolean): void;
    /**
     * Changes the z-index of the source's layer.
     *
     * @param {number} value
     * @memberof GoogleTileSource
     */
    setOrder(value: number): void;
    protected createOverlay(service: MapRequest, opts?: any): GoogleTileLayer;
    /**
     * Animation Providers
     */
    private _tileLoadCheckInterval;
    animationLayerForDate(animation: TileAnimation, date: Date, wait?: boolean): Promise<TiledLayer>;
}
export default GoogleTileSource;
