/// <reference types="leaflet" />
import MapLayer, { MapLayerOptions } from '../../../layers/MapLayer';
export interface LeafletLayerOptions extends MapLayerOptions {
    zIndex?: number;
}
/**
 * `LeafletLayer` is a container class that is responsible for creating and managing a Leaflet
 * layer representation on a Leaflet map.
 */
declare class LeafletLayer<T extends LeafletLayerOptions, S extends L.Layer> extends MapLayer<L.Map, T, S> {
    invalidate(): void;
    protected createRenderable(): S;
    protected updateVisibility(visible: boolean): void;
    protected addToMap(map: L.Map, index?: number): void;
    protected removeFromMap(map: L.Map): void;
}
export default LeafletLayer;
