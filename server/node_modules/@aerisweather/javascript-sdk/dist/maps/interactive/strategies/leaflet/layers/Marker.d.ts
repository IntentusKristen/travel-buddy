/// <reference types="leaflet" />
import { MarkerOptions, MarkerStyle } from '../../../../types';
import MapMarker, { MapMarkerOptions } from '../../../layers/MapMarker';
export interface LeafletMarkerOptions extends MapMarkerOptions {
}
declare class LeafletMarker extends MapMarker<L.Map, LeafletMarkerOptions, L.Marker> {
    update(opts: MarkerOptions): void;
    setStyle(style: MarkerStyle): void;
    protected createRenderable(): L.Marker;
    protected updateVisibility(visible: boolean): void;
    protected addToMap(map: L.Map, index?: number): void;
    protected removeFromMap(map: L.Map): void;
}
export default LeafletMarker;
