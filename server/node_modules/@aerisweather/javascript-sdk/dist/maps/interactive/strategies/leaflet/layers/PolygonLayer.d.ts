/// <reference types="leaflet" />
import { PolygonDef } from '../../../../types/layer.type';
import LeafletLayer from './Layer';
export interface LeafletPolygonLayerOptions extends L.PolylineOptions {
}
declare class LeafletPolygonLayer extends LeafletLayer<LeafletPolygonLayerOptions, L.Polyline> {
    private _polygons;
    constructor(layer: string, polygons: PolygonDef[], opts?: LeafletPolygonLayerOptions, id?: string);
    setOpacity(value: number): void;
    protected addToMap(map: L.Map, index?: number): void;
    protected removeFromMap(map: L.Map): void;
    protected createRenderable(): any;
}
export default LeafletPolygonLayer;
