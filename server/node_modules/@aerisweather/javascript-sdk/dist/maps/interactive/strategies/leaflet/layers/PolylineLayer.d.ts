/// <reference types="leaflet" />
import LeafletLayer from './Layer';
import { LatLonValue } from '../../../../../types';
export interface LeafletPolylineLayerOptions extends L.PolylineOptions {
}
declare class LeafletPolylineLayer extends LeafletLayer<LeafletPolylineLayerOptions, L.Polyline> {
    private _points;
    constructor(layer: string, points: LatLonValue[], opts?: LeafletPolylineLayerOptions, id?: string);
    setOpacity(value: number): void;
    protected addToMap(map: L.Map, index?: number): void;
    protected removeFromMap(map: L.Map): void;
    protected createRenderable(): any;
}
export default LeafletPolylineLayer;
