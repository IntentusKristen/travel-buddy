/// <reference types="mapbox-gl" />
import { MarkerOptions, MarkerStyle } from '../../../../types';
import MapMarker, { MapMarkerOptions } from '../../../layers/MapMarker';
export interface MapboxMarkerOptions extends MapMarkerOptions {
}
declare class MapboxMarker extends MapMarker<mapboxgl.Map, MapboxMarkerOptions, mapboxgl.Marker> {
    update(opts: MarkerOptions): void;
    setStyle(style: MarkerStyle): void;
    protected createRenderable(): mapboxgl.Marker;
    protected updateVisibility(visible: boolean): void;
    protected addToMap(map: mapboxgl.Map, index?: number): void;
    protected removeFromMap(map: mapboxgl.Map): void;
}
export default MapboxMarker;
