/// <reference types="mapbox-gl" />
import MapboxLayer from './Layer';
import { GeoBounds } from '../../../../../types';
export interface MapboxTileLayerOptions extends mapboxgl.RasterPaint {
    type?: string;
    url?: string;
    bounds?: GeoBounds;
    attribution?: string;
}
/**
 * `TileLayer` is a container class that is responsible for creating and managing a Mapbox
 * source and associated layer representation on a Mapbox map.
 */
declare class MapboxTileLayer extends MapboxLayer<MapboxTileLayerOptions, mapboxgl.Layer> {
    private _type;
    constructor(layer: string, opts: MapboxTileLayerOptions, id?: string);
    /**
     * Updates the tile or image url for the layer.
     *
     * @param {string} url
     * @memberof TileLayer
     */
    setUrl(url: string): void;
    /**
     * Updates the bounds for the layer when rendered as an image.
     *
     * @param {GeoBounds} bounds
     * @memberof TileLayer
     */
    setBounds(bounds: GeoBounds): void;
    /**
     * Updates the layer source using the specified image URL and coordinate bounds. Only used
     * for `image` layer types.
     *
     * @param {string} url
     * @param {GeoBounds} [bounds=undefined]
     * @returns
     * @memberof TileLayer
     */
    setImage(url: string, bounds?: GeoBounds): void;
    invalidate(): void;
    protected updateVisibility(visible: boolean): void;
    protected createRenderable(): any;
}
export default MapboxTileLayer;
