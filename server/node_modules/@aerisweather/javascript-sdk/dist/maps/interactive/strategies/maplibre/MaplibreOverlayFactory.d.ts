import * as Maplibre from '.';
import MapRequest from '../../../../network/maps/MapRequest';
import { Coordinate } from '../../../../types';
import { MarkerStyle, OverlayFactory, ShapeStyle } from '../../../types';
import { PolygonDef } from '../../../types/layer.type';
/**
 * A factory class that generates and returns renderable map objects from data, such as tile layers
 * and annotations, as required by the associated mapping strategy.
 *
 * @export
 * @class MapboxOverlayFactory
 * @implements {OverlayFactory}
 * @implements {PointDataSource}
 */
declare class MaplibreOverlayFactory implements OverlayFactory {
    /**
     * Returns a tile layer instance configured with the specified url template string and options.
     *
     * @param {string} layer
     * @param {MapRequest} service
     * @param {*} [opts]
     * @returns {TileSource}
     * @memberof OverlayFactory
     */
    tileLayer(layer: string, service: MapRequest, opts?: any): Maplibre.TileSource;
    /**
     * Returns an annotation, or marker, instance with the specified coordinate and configuration
     * options.
     *
     * @param {string} layer
     * @param {Coordinate} coord
     * @param {MarkerStyle} style
     * @returns {*}
     * @memberof OverlayFactory
     */
    marker(layer: string, coord: Coordinate, style: MarkerStyle): any;
    /**
     * Returns a polygon instance configured for the specified layer code and configuration.
     *
     * @param {string} layer
     * @param {Polygon[]} shapes
     * @param {ShapeStyle} style
     * @returns {*}
     * @memberof OverlayFactory
     */
    polygon(layer: string, shapes: PolygonDef[], style: ShapeStyle): Maplibre.Polygon;
    polyline(layer: string, points: Coordinate[], style: ShapeStyle): Maplibre.Polyline;
}
export default MaplibreOverlayFactory;
