/// <reference types="leaflet" />
import MapMarker, { MapMarkerOptions } from '../../../layers/MapMarker';
import Popup from '../Popup';
import { MarkerOptions, MarkerStyle } from '../../../../types';
export interface OpenLayersMarkerOptions extends MapMarkerOptions {
}
declare class OpenLayersMarker extends MapMarker<ol.Map, OpenLayersMarkerOptions, ol.layer.Vector> {
    private _feature;
    private _popup;
    private _activePopup;
    get feature(): ol.Feature;
    get popup(): Popup;
    update(opts: MarkerOptions): void;
    setStyle(style: MarkerStyle): void;
    setPopup(popup: Popup): void;
    protected createRenderable(): ol.layer.Vector;
    protected updateVisibility(visible: boolean): void;
    protected addToMap(map: ol.Map, index?: number): void;
    protected removeFromMap(map: ol.Map): void;
}
export default OpenLayersMarker;
