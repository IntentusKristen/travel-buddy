/// <reference types="openlayers" />
import OpenLayersLayer, { OpenLayersLayerOptions } from './Layer';
import { GeoBounds } from '../../../../../types';
export interface OpenLayersTileLayerOptions extends OpenLayersLayerOptions {
    type?: string;
    url?: string;
    bounds?: GeoBounds;
    attribution?: string;
}
/**
 * `TileLayer` is a container class that is responsible for creating and managing a OpenLayers
 * source and associated layer representation on a OpenLayers map.
 */
declare class OpenLayersTileLayer extends OpenLayersLayer<OpenLayersTileLayerOptions, ol.layer.Tile> {
    private _source;
    private _type;
    private _loadCounter;
    /**
     * Updates the tile or image url for the layer.
     *
     * @param {string} url
     * @memberof TileLayer
     */
    setUrl(url: string): void;
    /**
     * Updates the bounds for the layer when rendered as an image.
     *
     * @param {GeoBounds} bounds
     * @memberof TileLayer
     */
    setBounds(bounds: GeoBounds): void;
    /**
     * Forces a redraw of the layer on the map.
     *
     * @returns
     * @memberof TileLayer
     */
    invalidate(): void;
    protected createRenderable(): any;
}
export default OpenLayersTileLayer;
