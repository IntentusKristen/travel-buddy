import { TimeValueRepresentable } from '.';
/**
 * A data type that represents a series of `TimeValueRepresentable` objects keyed by unique
 * identifier.
 */
export declare type TextAnimationData = Record<string, TimeValueRepresentable>;
