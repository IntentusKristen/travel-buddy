import Geometry from '../geo/Geometry';
import MapElement from '../interactive/MapElement';
export declare type AnyMapElement = MapElement<Geometry, any>;
export declare type MapElementGroup<T extends AnyMapElement> = {
    data: any;
    elements: Array<T>;
};
export declare type CalloutFormatter = () => string;
