import Legend from './Legend';
import { Size } from '../../../types';
export interface ImageLegendOptions {
    title?: string;
    url?: string | {
        imperial: string;
        metric: string;
    };
}
/**
 * An image legend is an object that is responsible for displaying and managing multiple related
 * legends within a single view along with title labels for each as required.
 *
 * @export
 * @class ImageLegend
 * @implements {LegendRenderable}
 */
declare class ImageLegend extends Legend {
    private _image;
    get image(): HTMLImageElement;
    url(): string;
    getSize(): Size;
    /**
     * Resets the legend by removing all existing images.
     *
     * @memberof ImageLegend
     */
    reset(): void;
    update(opts?: any, force?: boolean): void;
    private _load;
}
export default ImageLegend;
