import Collection from '../../../core/Collection';
import { LegendRenderable } from '../../types/legend.interface';
declare class LegendStore extends Collection<LegendRenderable> {
    private _mapping;
    private _refs;
    get mapping(): Map<string, string[]>;
    get refs(): Map<string, number>;
    add(key: string, item: LegendRenderable): void;
    has(key: string): boolean;
    remove(key: string, clear?: boolean): void;
    map(key: string, mapping: string): void;
    increment(key: string): void;
    decrement(key: string): void;
    refCount(key: string): number;
}
export default LegendStore;
