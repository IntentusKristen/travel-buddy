import Animation from '../../anim/Animation';
import { ImageAnimationProvider } from '../../anim/ImageAnimation';
import Timeline from '../../anim/Timeline';
import { MapViewAnimatorOptions } from '../../types';
declare type AnimationTargets = {
    past: HTMLElement;
    future: HTMLElement;
    pastText: HTMLElement;
    futureText: HTMLElement;
};
declare class MapViewAnimator {
    private _targets;
    private _opts;
    private _timeline;
    get timeline(): Timeline;
    set provider(provider: ImageAnimationProvider);
    get past(): Animation;
    get future(): Animation;
    get pastText(): Animation;
    get futureText(): Animation;
    constructor(targets: AnimationTargets, opts: MapViewAnimatorOptions);
    private init;
}
export default MapViewAnimator;
