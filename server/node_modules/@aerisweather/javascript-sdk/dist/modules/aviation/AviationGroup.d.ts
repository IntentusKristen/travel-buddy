import ModuleGroup from '../ModuleGroup';
import IMapSourceModule from '../interfaces/IMapSourceModule';
/**
 * The Aviation module contains a series of modules that are specific to aviation usage.
 *
 * Includes:
 * - flight rules
 * - SIGMETs
 * - station plots
 *
 * @class AviationModule
 * @extends {ModuleGroup}
 */
declare class AviationGroup extends ModuleGroup {
    get id(): string;
    controls(): any;
    load(): Promise<IMapSourceModule[]>;
}
export default AviationGroup;
