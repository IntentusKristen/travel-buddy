declare class StationPlotView {
    private _obs;
    private _legend;
    constructor(obs: any);
    render(zoom: number, legend?: boolean): string;
    symbol(obs: any): string;
    values(obs: any): string;
    legend(obs: any): string;
}
export default StationPlotView;
