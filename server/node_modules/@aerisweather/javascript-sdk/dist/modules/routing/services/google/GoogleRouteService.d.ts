import { Coordinate } from '../../../../types';
import { RouteService, RouteResult } from '../../interfaces/RouteService';
interface GoogleRouteOptions {
    accessToken: string;
}
declare class GoogleRouteService implements RouteService {
    private opts;
    private cancelToken;
    private directionsService;
    constructor(opts: GoogleRouteOptions);
    getDirections(points: Coordinate[] | string[], params?: any): Promise<RouteResult>;
    geocode(search: string): Promise<Coordinate>;
    cancel(): void;
}
export default GoogleRouteService;
