import { Coordinate } from '../../../../types';
import { RouteService, RouteResult } from '../../interfaces/RouteService';
interface MapboxRouteOptions {
    accessToken: string;
}
declare class MapboxRouteService implements RouteService {
    private opts;
    private cancelToken;
    constructor(opts: MapboxRouteOptions);
    getDirections(points: Coordinate[] | string[], params?: any): Promise<RouteResult>;
    geocode(search: string): Promise<Coordinate>;
    cancel(): void;
}
export default MapboxRouteService;
