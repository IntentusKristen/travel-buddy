import { CancelToken as AxiosCancelToken, CancelTokenSource } from 'axios';
export declare const isCancel: (e: any) => boolean;
export declare const createCancelToken: () => {
    token: AxiosCancelToken;
    source: CancelTokenSource;
    cancel: () => void;
    isCancel: (e: any) => boolean;
};
/**
 * An `Http` object is an adapter that performs HTTP requests using the underlying request library.
 */
declare class Http {
    private _client;
    private _source;
    /**
     * Initializes an instance with the baseUrl and header configuration. The `baseUrl` value will
     * be prepended to all relative urls made with this instance, whereas absolute urls will be
     * passed through without prefixing.
     *
     * @param baseUrl
     * @param headers
     */
    constructor(baseUrl: string, headers?: any);
    /**
     * Performs a GET request for the relative or absolute url.
     *
     * @param {string} url
     * @param {*} [opts=undefined]
     * @returns
     * @memberof Http
     */
    get(url: string, opts?: any): Promise<import("axios").AxiosResponse<any>>;
    /**
     * Performs a POST request for the relative or absolute url.
     *
     * @param {string} url
     * @param {*} data
     * @param {*} [opts=undefined]
     * @returns
     * @memberof Http
     */
    post(url: string, data: any, opts?: any): Promise<import("axios").AxiosResponse<any>>;
    cancel(): void;
    /**
     * A class method that performs a request to an absolute url.
     *
     * @param url
     * @param opts
     */
    static request(url: string, opts?: any): Promise<import("axios").AxiosResponse<any>>;
}
export default Http;
