import { Coordinate, GeoBounds } from '../types';
/**
 * Generates a coordinate bounds object type from the array of coordinates.
 *
 * @param coords
 * @returns {GeoBounds}
 */
export declare const boundsFromCoords: (coords: Coordinate[]) => GeoBounds;
